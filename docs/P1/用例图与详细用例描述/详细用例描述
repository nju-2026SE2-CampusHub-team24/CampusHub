### 核心用例详细描述

#### 1. 核心用例一：发布互助任务
*   **用例名称：** 发布互助任务
*   **参与者：** 已认证学生（作为需求发布方）
*   **前置条件：** 用户已登录，已通过“校园实名认证”，且当前账户信用分高于限制阈值（如 ≥60分）。
*   **基本流（正常流程）：**
    1. 用户在平台首页点击“+ 发布需求”按钮。
    2. 用户选择任务类别（快递跑腿、二手交易、学习辅导、组队匹配）。
    3. 系统展示对应类别的表单。用户填写任务标题、详情、截止时间、交接地点。
    4. 用户设定悬赏（输入愿意支付的积分或接入支付输入金额）。
    5. 用户点击“确认发布”。系统将悬赏金/积分进行**平台预扣托管**。
    6. 系统进行自动化违禁词检测，检测通过后将需求卡片展示在需求大厅中。
*   **替代流（异常或分支流程）：**
    *   *4a. 余额/积分不足：* 系统提示用户余额不足，跳转至充值或积分获取页面。
    *   *6a. 包含敏感词/违规词汇：* 系统拦截发布请求，提示“包含违规内容”，要求用户修改后重新提交。
*   **后置条件：** 系统在数据库中生成一条新的订单记录，状态标记为 `待接单`，预扣用户的悬赏进入托管账户。

#### 2. 核心用例二：接受互助任务
*   **用例名称：** 接受互助任务
*   **参与者：** 已认证学生（作为任务接单方）
*   **前置条件：** 用户已登录且完成认证；信用分符合接单标准；目标任务状态必须为 `待接单`。
*   **基本流（正常流程）：**
    1. 用户在需求大厅浏览，点击感兴趣的任务卡片进入详情页。
    2. 用户查看具体需求内容及**发布者的历史评价与信用分**（响应访谈对象2、3的需求）。
    3. 用户点击“立即接单”按钮。
    4. 系统验证接单合法性，锁定该订单，状态变更为 `进行中`。
    5. 系统给发布者推送通知“您的任务已被接取”。
    6. 系统自动为双方创建站内私信会话（保护隐私，响应问卷第12题），并解锁隐藏的敏感信息（如取件码原图）。
*   **替代流（异常或分支流程）：**
    *   *3a. 任务已被抢单：* 当用户点击接单时，系统若判定已被他人提前0.1秒抢单，则弹窗提示“手慢了，该任务已被接取”，刷新页面。
    *   *3b. 接单上限限制：* 为防恶意刷单（响应问卷防范机制），系统若检测到用户当前进行中订单超过 5 单，提示“进行中任务过多，请完成后再接单”。
*   **后置条件：** 任务状态变更为 `进行中`，接单人与发单人身份绑定，开启私信沟通渠道。

#### 3. 核心用例三：确认任务完成与双向互评
*   **用例名称：** 确认任务完成与评价
*   **参与者：** 已认证学生（发布方与接单方）
*   **前置条件：** 接单方已执行完任务，点击了“提交完成”并上传了凭证（如包裹放在寝室门口的照片），订单状态变更为 `待确认完成`。
*   **基本流（正常流程）：**
    1. 系统向发布方发送“接单人已完成任务，请确认”的推送通知。
    2. 发布方查看站内信及接单方上传的完成凭证照片。
    3. 发布方核实验收无误后，点击“确认完成”。
    4. 系统将托管的悬赏（积分或金额）正式结算，打入接单方账户。
    5. 订单状态变更为 `已完成`，系统弹窗邀请双方进行 1~5 星打分并填写评语。
    6. 双方提交评价后，系统自动更新双方的“历史评价记录”及“信用分”。
*   **替代流（异常或分支流程）：**
    *   *2a. 任务未达标产生纠纷：* 发布方核实发现任务未完成或有损坏，点击“驳回”。系统提示双方先私信沟通；若沟通无果，任一方可点击“申请客服仲裁”，订单进入 `申诉中` 状态，等待管理员（Admin）介入。
    *   *3a. 发布方超时未确认：* 接单方提交完成后，若发布方超过 24 小时未确认且未驳回，系统将**自动确认完成**并结算悬赏（响应问卷第12题的拖延防止机制）。
*   **后置条件：** 订单闭环，财务/积分完成转移，双方信用档案数据得到更新。